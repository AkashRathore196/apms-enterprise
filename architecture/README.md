# Architecture

This directory contains APMS architecture governance, baselines, decision records and traceability artifacts.

The enterprise architecture remains frozen at the approved baseline. Domain implementation must follow the established capability-first validation lifecycle.
