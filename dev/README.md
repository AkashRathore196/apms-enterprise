# Developer Runtime

The `dev/` area contains reproducible local development bootstrap assets.

Reference local services:

PostgreSQL · Kafka · Keycloak · Kong · Camunda · OpenSearch · OpenTelemetry

Local credentials are development-only and must never be committed as real secrets.
