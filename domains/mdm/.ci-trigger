CI trigger marker for the isolated MDM eventing acceptance workflows.

The canonical PR acceptance gates are registered on main and run from the main-branch workflow definition against the PR head.
No runtime code or production behavior is changed.